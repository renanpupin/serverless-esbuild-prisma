before_compile:
  - echo "Before compile"
  - n 16.16.0
  - printenv

before_build:
  - echo "Before build"
#  - cp prisma/schema.prisma ./
#  - npx prisma generate --schema=src/generated/client/schema.prisma